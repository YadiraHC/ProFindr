import React from 'react';

const HowItWorks: React.FC = () => {
    return (
        <section>
            {/* Código de la sección de cómo funciona */}
        </section>
    );
};

export default HowItWorks;
